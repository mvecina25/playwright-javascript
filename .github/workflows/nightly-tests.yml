name: 🌙 Nightly Regression

on:
  schedule:
    # /**
    #  * WHY: We schedule the suite for 13:00 UTC (8:00 AM EST / 9:00 AM EDT) to ensure the 
    #  * team has fresh results at the start of the workday.
    #  */
    - cron: '0 13 * * *'
    
  workflow_dispatch:
    # /**
    #  * WHY: workflow_dispatch enables manual triggering for debugging 
    #  * specific environments or running targeted test sets via grep.
    #  */
    inputs:
      environment_url:
        description: 'Target Application URL (Falls back to Repo Variable if empty)'
        required: false
        default: ''
      grep_tag:
        description: 'Playwright Grep Tag'
        required: false
        default: '@nightly'

# WHY: Specific permissions are required for the bot to write the report to the gh-pages branch.
permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  nightly-test:
    name: Execute Nightly Full Suite
    timeout-minutes: 60
    runs-on: ubuntu-latest
    
    steps:
    - name: GIVEN the repository source code is checked out
      uses: actions/checkout@v4

    - name: AND the Node.js environment is initialized
      uses: actions/setup-node@v4
      with:
        node-version: 20
        # WHY: npm caching significantly reduces build time by reusing stored modules
        cache: 'npm'

    - name: WHEN the project dependencies are installed
    #   /**
    #    * WHY: --force bypasses peer dependency conflicts. 
    #    * --omit=optional prevents platform-specific errors (like fsevents on Linux) 
    #    * which commonly cause EBADPLATFORM failures in CI.
    #    */
      run: npm install --force --omit=optional

    - name: AND the Playwright browser binaries are provisioned
      # WHY: We specify 'chromium' to reduce setup time and artifact size in the runner
      run: npx playwright install --with-deps chromium

    - name: THEN the automated nightly regression suite is executed
    #   /**
    #    * WHY: We use the grep tag to filter the nightly subset. 
    #    * APP_BASE_URL prioritizes manual input before falling back to a Repo Variable.
    #    * WHY: continue-on-error: true is critical; if tests fail, we still need to generate 
    #    * the report so the failures can be analyzed in Allure.
    #    */
      run: npx playwright test --grep ${{ github.event.inputs.grep_tag || '@nightly' }} --project=chromium
      continue-on-error: true
      env:
        APP_BASE_URL: ${{ github.event.inputs.environment_url || vars.APP_BASE_URL }}

    - name: AND the Allure report is generated from raw results
      # WHY: We use 'npx' to call the allure-commandline tool defined in your package.json devDependencies.
      if: always()
      run: npx allure generate allure-results --clean -o allure-report

    - name: AND the report is deployed to GitHub Pages for live viewing
      # WHY: GitHub does not render HTML in artifacts. Deploying to GH Pages provides a shareable dashboard.
      # We use the run_number to keep a history of nightly reports.
      uses: peaceiris/actions-gh-pages@v3
      if: always()
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./allure-report
        destination_dir: nightly-reports/${{ github.run_number }}

    - name: THEN the interactive report link is published to the Job Summary
      # WHY: This provides an immediate, clickable entry point for stakeholders in the GitHub UI.
      if: always()
      run: |
        echo "### 📊 Allure Nightly Report" >> $GITHUB_STEP_SUMMARY
        echo "View the interactive results for run #${{ github.run_number }} here:" >> $GITHUB_STEP_SUMMARY
        echo "https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/nightly-reports/${{ github.run_number }}/" >> $GITHUB_STEP_SUMMARY

    - name: AND the test execution report is preserved as a build artifact
    #   /**
    #    * WHY: Ensures that even if tests fail (which is an expected 
    #    * outcome of a regression suite), the HTML report is preserved for debugging.
    #    */
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: nightly-test-report
        path: playwright-report/
        retention-days: 30